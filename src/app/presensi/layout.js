export const viewport = {
  width: "device-width",
  initialScale: 1.0,
  maximumScale: 1.0,
  userScalable: false,
};

export default function PresensiLayout({ children }) {
  return <>{children}</>;
}
